import React from "react";
import Layout from "../../hoc/Layout/Layout"

const tuitionIndexPage = (props) => (
  <Layout>
    <h2>The Events Index Page</h2>
    <p>Hi there</p>
  </Layout>
);

export default tuitionIndexPage;
